type User @model {
  id: ID!
  firstName: String
  lastName: String
  description: String
  image: String
}

type Tournament @model {
  id: ID!
  author: ID
  tournamentName: String!
  startTime: String!
  maximumNumberOfParticipants: Int!
}